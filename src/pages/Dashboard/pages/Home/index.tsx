import React, { FunctionComponent } from 'react';

interface indexProps {
  prop: string;
}

const Home: FunctionComponent = ( ) => {

    return (
        <div>
            home
        </div>
    );
};

export default Home;
